node_modules/
dist/
*.log
.DS_Store

# canfail crash journal and backups: transient, must never be committed
.canfail-journal.json
.canfail-backup/
*.canfail-hidden

# Local Kiro session memory - never commit
.kiro-memory/

# Local editor and traffic-capture artifacts - never commit
.kiro/settings/
mitm_mcp_traffic.db
